import theme from "../gatsby-plugin-theme-ui";
import { Text } from "theme-ui";
const themeJSONString = JSON.stringify(theme, null, 2);

# Theme JSON

The theme JSON is below:

<pre>{themeJSONString}</pre>
